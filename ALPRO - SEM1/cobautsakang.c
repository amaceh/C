#include <stdio.h>

int lin,col, index;

scanf("%d", &n)

for (index = 0; index < n; index+=i)
{
	for (lin = 1; lin <= star; lin+=1){
		for (col = 1; col <= lin; col+=1){
			printf("0");
		}
return 0;
}